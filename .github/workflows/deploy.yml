name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy: 
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4
      
      - name: Build Docker
        run: docker compose build --no-cache

      - name: Start Docker
        run: docker compose up -d